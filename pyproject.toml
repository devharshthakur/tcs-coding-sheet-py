[project]
name = "tcs-coding-sheet-py"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"

dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.6",
    "ty>=0.0.23",
]
